# spec-check allowlist for deferred decision backfills.
#
# Every R-* requirement defined in specs/NNNN-*/requirements.md must
# carry at least one decisions/DEC-*.md file whose front-matter
# `requirement:` field names that ID. R-* IDs listed below under
# `deferred:` are deferred backfills: the requirements shipped before
# the matching DEC file landed; the DEC lands in a later pass.
#
# Format: a YAML list of R-* IDs under the `deferred` key. Each entry
# carries a short note for context.
#
# Removing an entry from this list requires landing the matching DEC
# in the same commit.

deferred:
  # The only R-* IDs defined in specs/*/requirements.md before the
  # Phase D run-evidence rollout were R-CDCP-001..010 (covered
  # collectively by DEC-CDCP-001 via the bootstrap exemption rule in
  # scripts/spec_check.py) plus R-FIN-001..002 (covered by
  # DEC-FIN-001 and DEC-FIN-002 respectively). The five
  # architectural DECs that shipped in the install pass
  # (DEC-MAP-001 through DEC-MAP-005) carry placeholder requirement
  # IDs R-MAP-001..R-MAP-005; those IDs are not defined as
  # requirements yet, so they do not trigger the spec_check coverage
  # rule.
  #
  # Phase D / 2026-05-27 added R-FIN-003..008 for the watchlist
  # export run-evidence emission. R-FIN-003 is resolved by
  # DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  # directly via the front-matter requirement field. R-FIN-004..008
  # are resolved collectively by the same DEC; the DEC's
  # `## coverage` section enumerates them. This mirrors the
  # supplier-risk-rag-agent allowlist pattern for DEC-EVL-006 +
  # R-EVL-007..011 (Phase D precedent).
  - id: R-FIN-004
    note: covered collectively by DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  - id: R-FIN-005
    note: covered collectively by DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  - id: R-FIN-006
    note: covered collectively by DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  - id: R-FIN-007
    note: covered collectively by DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  - id: R-FIN-008
    note: covered collectively by DEC-FIN-003-watchlist-export-emits-conformant-run-evidence
  # Round 3 / 2026-05-28 added R-FIN-009..012 for the emitter rename
  # plus validator cross-checks. R-FIN-009 is resolved by
  # DEC-FIN-004-watchlist-export-run-evidence-cross-checks directly
  # via the front-matter requirement field. R-FIN-010..012 are
  # resolved collectively by the same DEC; the DEC's `## coverage`
  # section enumerates them.
  - id: R-FIN-010
    note: covered collectively by DEC-FIN-004-watchlist-export-run-evidence-cross-checks
  - id: R-FIN-011
    note: covered collectively by DEC-FIN-004-watchlist-export-run-evidence-cross-checks
  - id: R-FIN-012
    note: covered collectively by DEC-FIN-004-watchlist-export-run-evidence-cross-checks
  # Round 5 / 2026-05-28 added R-FIN-013..016 for the deterministic
  # replay command. R-FIN-013 is resolved by
  # DEC-FIN-005-watchlist-replay-command directly via the
  # front-matter requirement field. R-FIN-014..016 are resolved
  # collectively by the same DEC; the DEC's `## coverage` section
  # enumerates them.
  - id: R-FIN-014
    note: covered collectively by DEC-FIN-005-watchlist-replay-command
  - id: R-FIN-015
    note: covered collectively by DEC-FIN-005-watchlist-replay-command
  - id: R-FIN-016
    note: covered collectively by DEC-FIN-005-watchlist-replay-command
  # Round 6 / 2026-05-29 added R-FIN-017..020 for the portable
  # repo:// URI migration plus the sandbox_image_ref off-by-one
  # fix. R-FIN-017 is resolved by
  # DEC-FIN-006-watchlist-export-portable-repo-uri-migration
  # directly via the front-matter requirement field. R-FIN-018..020
  # are resolved collectively by the same DEC; the DEC's
  # `## coverage` section enumerates them.
  - id: R-FIN-018
    note: covered collectively by DEC-FIN-006-watchlist-export-portable-repo-uri-migration
  - id: R-FIN-019
    note: covered collectively by DEC-FIN-006-watchlist-export-portable-repo-uri-migration
  - id: R-FIN-020
    note: covered collectively by DEC-FIN-006-watchlist-export-portable-repo-uri-migration
  # Round 7 Phase 2 / 2026-05-29 added R-FIN-021..024 for the CI
  # workflow set that enforces the DEC-CDCP-015 run-evidence chain
  # contract. R-FIN-021 is resolved by
  # DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain
  # directly via the front-matter requirement field. R-FIN-022..024
  # are resolved collectively by the same DEC; the DEC's
  # `## coverage` section enumerates them.
  - id: R-FIN-022
    note: covered collectively by DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain
  - id: R-FIN-023
    note: covered collectively by DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain
  - id: R-FIN-024
    note: covered collectively by DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain
  # Workflow B recovery / 2026-05-29 added R-FIN-025..027 for the
  # dedicated multi-rerun determinism fixture plus the per-replay
  # ledger UUID-suffix fix plus the replay-determinism CI job.
  # R-FIN-025 is resolved by
  # DEC-FIN-008-chip-map-dedicated-determinism-fixture directly via
  # the front-matter requirement field. R-FIN-026..027 are resolved
  # collectively by the same DEC; the DEC's `## coverage` section
  # enumerates them.
  - id: R-FIN-026
    note: covered collectively by DEC-FIN-008-chip-map-dedicated-determinism-fixture
  - id: R-FIN-027
    note: covered collectively by DEC-FIN-008-chip-map-dedicated-determinism-fixture
  # 2026-05-29 added R-FIN-028..030 for the two new scenarios
  # (cowos-l-bottleneck + lithography-equipment-constraint) plus the
  # scoring change that folds the scenario lead-time bumps and
  # edge-strength bumps. R-FIN-028 is resolved by
  # DEC-FIN-009-additional-scenarios directly via the front-matter
  # requirement field. R-FIN-029..030 are resolved collectively by
  # the same DEC; the DEC's `## coverage` section enumerates them.
  - id: R-FIN-029
    note: covered collectively by DEC-FIN-009-additional-scenarios
  - id: R-FIN-030
    note: covered collectively by DEC-FIN-009-additional-scenarios
