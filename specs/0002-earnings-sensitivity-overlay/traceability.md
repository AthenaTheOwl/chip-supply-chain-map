# traceability: earnings sensitivity overlay

| Requirement | Design surface | Decision | Planned proof | Owner role |
|---|---|---|---|---|
| R-FIN-001 | `src/data/financial_sensitivity.csv`, `src/lib/financial.ts`, `src/components/NodeDetailPanel.tsx`, `src/data/sources.md` | `DEC-FIN-001-static-sourced-sensitivity-over-live-market-data.md` | full python gate list plus `npm run build` | `owner_role: engineering.implementation` |
| R-FIN-002 | `src/lib/riskPacket.ts`, `src/components/WatchlistPanel.tsx`, `src/state/store.ts`, `src/components/NodeDetailPanel.tsx` | `DEC-FIN-002-deterministic-watchlist-risk-packets.md` | `npm test`, full python gate list, `npm run build`, `npm run lint` | `owner_role: engineering.implementation` |
| R-FIN-003 | `src/lib/runEvidence.ts` `emitEvent` plus the per-step event emission in `scripts/export_watchlist/main.ts` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | a pipeline execution's `ops/event-ledger/<run-id>.jsonl` validates against `event.schema.json` | `owner_role: science.proof-gate-runner` |
| R-FIN-004 | `src/lib/runEvidence.ts` `emitRun` plus the Run record assembly in `scripts/export_watchlist/main.ts` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | a pipeline execution's `ops/run-records/<run-id>.json` validates against `run.schema.json` | `owner_role: science.proof-gate-runner` |
| R-FIN-005 | `canonicalizeInputs` + `canonicalizeHeuristicConfig` + `buildRunEvidenceFields` in `src/lib/runEvidence.ts` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | each Run record carries SHA-256 hex digests in `prompt_snapshot_hash` and `tool_schemas_snapshot_hash` | `owner_role: science.proof-gate-runner` |
| R-FIN-006 | `deriveSandboxImageRef` in `src/lib/runEvidence.ts` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | each Run record carries `sandbox_image_ref` as `<repo-path>@<HEAD-SHA>` | `owner_role: science.proof-gate-runner` |
| R-FIN-007 | `aggregateGateResults` in `src/lib/runEvidence.ts` plus the `gate.check.*` emission in `scripts/export_watchlist/main.ts` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | the pipeline's `gate.check.*` event names match the `gate_results_summary` lists in the Run record | `owner_role: science.proof-gate-runner` |
| R-FIN-008 | `scripts/validate_run_evidence.py` + the `validate_run_evidence` step in `.github/workflows/gates.yml` | `DEC-FIN-003-watchlist-export-emits-conformant-run-evidence.md` | a CI run shows the gate's pass output and a schema-violating record produces a red gate | `owner_role: control.coordinator` |
| R-FIN-009 | the two `tool.call.completed` emissions in `scripts/export_watchlist/main.ts` (`computeChokepointScores`, `buildWatchlistRiskPacket`) plus the `tool.call.completed` branch in `ops/schemas-cache/event.schema.json` | `DEC-FIN-004-watchlist-export-run-evidence-cross-checks.md` | the regenerated sample ledger carries `tool_name` on both `tool.call.completed` lines and the schema branch rejects payloads missing the key | `owner_role: control.coordinator` |
| R-FIN-010 | the `gate.run.evidence_recorded` emissions in `scripts/export_watchlist/main.ts` plus the matching branch in `ops/schemas-cache/event.schema.json` | `DEC-FIN-004-watchlist-export-run-evidence-cross-checks.md` | the regenerated sample ledger carries `fields_populated` on the evidence_recorded line and the schema branch rejects payloads missing the key | `owner_role: control.coordinator` |
| R-FIN-011 | the `pipeline.done` emission in `scripts/export_watchlist/main.ts` (clones `run.gate_results_summary` into the payload) | `DEC-FIN-004-watchlist-export-run-evidence-cross-checks.md` | the regenerated sample ledger's `pipeline.done` payload carries the same `gates_passed`/`gates_failed`/`all_passed` shape as the Run record's `gate_results_summary` | `owner_role: control.coordinator` |
| R-FIN-012 | the `cross_check` function in `scripts/validate_run_evidence.py` plus the test matrix in `scripts/test_validate_run_evidence.py` | `DEC-FIN-004-watchlist-export-run-evidence-cross-checks.md` | the test module covers one positive case plus seven negative cases (six core failure modes plus an `all_passed`-flag disagreement) | `owner_role: control.coordinator` |
| R-FIN-013 | the `verify_head` function in `scripts/replay_run.py` plus the `test_head_mismatch_exits_one` case in `scripts/test_replay_run.py` | `DEC-FIN-005-watchlist-replay-command.md` | the script exits 1 with the canonical `git checkout <sha>` message when HEAD does not match the recorded sandbox SHA | `owner_role: control.coordinator` |
| R-FIN-014 | the `verify_inputs` function plus `_canonicalize_inputs` and `_canonicalize_heuristic_config` in `scripts/replay_run.py`, plus the `test_input_mutation_detected` case in `scripts/test_replay_run.py` | `DEC-FIN-005-watchlist-replay-command.md` | a mutated input file produces a `tool_schemas_snapshot_hash` mismatch, the replay exits 1, and the replay record carries `verdict: input_hash_mismatch` | `owner_role: control.coordinator` |
| R-FIN-015 | the `run_export` + `compare_outputs` functions in `scripts/replay_run.py` plus the `test_positive_replay_equivalent_true` case in `scripts/test_replay_run.py` | `DEC-FIN-005-watchlist-replay-command.md` | the replay shell-outs to `node scripts/export_watchlist.mjs --no-emit-evidence --output=<tmp>`, hashes the produced packet, and the canonical replay record at `ops/replay-records/run-6a665b303138/` carries equal `replayed_packet_hash` and `committed_packet_hash` | `owner_role: control.coordinator` |
| R-FIN-016 | the `emit_replay_event` + `write_replay_record` functions in `scripts/replay_run.py` plus the canonical artifact pair under `ops/event-ledger/replay-run-6a665b303138-*.jsonl` and `ops/replay-records/run-6a665b303138/` | `DEC-FIN-005-watchlist-replay-command.md` | the canonical artifact pair exists at the documented paths and carries `replay_equivalent: true` + `replay_method: deterministic` + a non-empty `packet_ref` | `owner_role: control.coordinator` |
| R-FIN-017 | `REPO_NAME` + `buildRepoUri` + `buildArtifactUri` + `pendingSandboxImageRef` exported from `src/lib/runEvidence.ts`, plus the Run record assembly in `scripts/export_watchlist/main.ts` that wires the URI shape into `sandbox_image_ref`, `inputs[].ref`, `outputs[].artifact_id`, and `workspace_id` | `DEC-FIN-006-watchlist-export-portable-repo-uri-migration.md` | the regenerated sample at `ops/run-records/run-6a665b303138.json` carries `sandbox_image_ref: repo://chip-supply-chain-map@<sha>/`, every input ref starts with `repo://chip-supply-chain-map@<sha>/`, every output artifact_id starts with `artifact://chip-supply-chain-map/`, and `workspace_id: chip-supply-chain-map` | `owner_role: control.coordinator` |
| R-FIN-018 | the `resolve_uri` helper + `_REPO_URI_RE` + `_ARTIFACT_URI_RE` constants in `scripts/validate_run_evidence.py` plus the `ResolveUriTests` case in `scripts/test_validate_run_evidence.py` | `DEC-FIN-006-watchlist-export-portable-repo-uri-migration.md` | the test class covers the four interop cases (repo URI, artifact URI, legacy path, malformed URI) plus the PENDING placeholder branch plus a URI Run record validating clean end-to-end | `owner_role: control.coordinator` |
| R-FIN-019 | the updated `_parse_sandbox_sha` parser + `resolve_uri` helper + PENDING-refusal branch in `scripts/replay_run.py` plus the `ReplayUriTests` case in `scripts/test_replay_run.py` | `DEC-FIN-006-watchlist-export-portable-repo-uri-migration.md` | the test class covers four parser branches (URI form, URI with trailing path, legacy form, PENDING) plus three `resolve_uri` cases (repo, artifact, legacy) | `owner_role: control.coordinator` |
| R-FIN-020 | `scripts/finalize_sandbox_ref.py` plus the four-case test matrix in `scripts/test_finalize_sandbox_ref.py` | `DEC-FIN-006-watchlist-export-portable-repo-uri-migration.md` | the finalizer rewrites every `@PENDING/` token in `sandbox_image_ref` and `inputs[].ref` to the supplied head SHA, is idempotent on already-resolved records, exits 1 on missing files, and supports partial rewrites | `owner_role: control.coordinator` |
| R-FIN-021 | `.github/workflows/gates.yml`, `.github/workflows/build.yml`, `.github/workflows/run-evidence-gates.yml` (all carry `on.pull_request:` + `on.push.branches: [main]`, all jobs `runs-on: ubuntu-latest`, Python 3.11, Node 20) | `DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain.md` | a CI run on a PR targeting main and a CI run on a push to main both fire the full workflow set on Linux with the pinned Python and Node versions | `owner_role: control.coordinator` |
| R-FIN-022 | the universal-gate step list in `.github/workflows/gates.yml`, the `npm test` step in `.github/workflows/build.yml`, and the packet-generation + packet-validation + replay-smoke steps in `.github/workflows/run-evidence-gates.yml` | `DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain.md` | a CI run shows green on each DEC-CDCP-015 contract gate (schema cache freshness, voice lint, BOM, spec check, decisions, validate_run_evidence, Python + TS test runners, packet generation, packet validation, replay smoke) | `owner_role: control.coordinator` |
| R-FIN-023 | the `Extract recorded sandbox SHA` + `replay-smoke` steps in `.github/workflows/run-evidence-gates.yml` (jq + `grep -oE '[a-f0-9]{40}'` extraction, `fetch-depth: 0` checkout, save-restore of the finalized Run record across the `git checkout <sandbox-sha>`, then `python scripts/replay_run.py --run-id run-6a665b303138`) | `DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain.md` | the replay-smoke step exits 0 with `replay_equivalent: true` against the recorded sandbox SHA `86d22482fb8a7425b3edfad99020f83deefec734` | `owner_role: control.coordinator` |
| R-FIN-024 | absence of `continue-on-error: true`, `if: ${{ failure() }}`, and `if: always()` on any contract step across the three workflow files | `DEC-FIN-007-chip-supply-chain-map-ci-enforces-run-evidence-chain.md` | a grep over `.github/workflows/*.yml` returns zero matches for `continue-on-error: true` or the failure-masking conditionals on any contract step | `owner_role: control.coordinator` |
| R-FIN-025 | `scripts/test_replay_determinism.py` (Python unittest fixture that replays the canonical sample RERUNS times, extracts the three replay-equivalence hashes from each `ops/replay-records/run-6a665b303138/<replay-event-id>.json`, canonicalizes the tuple, and asserts SHA-256 agreement across every rerun plus per-rerun `verdict == "byte_equal"` plus `replayed_packet_hash == committed_packet_hash`) | `DEC-FIN-008-chip-map-dedicated-determinism-fixture.md` | `python -m unittest scripts.test_replay_determinism` passes against the canonical sample with `RERUNS=3` and produces one canonical hash across every rerun | `owner_role: control.coordinator` |
| R-FIN-026 | the `emit_replay_event` function in `scripts/replay_run.py` (ledger filename `replay-<run-id>-<safe-ts>-<replay-event-id>.jsonl` suffixes the per-replay UUID) | `DEC-FIN-008-chip-map-dedicated-determinism-fixture.md` | two consecutive replays inside the determinism fixture write to distinct per-replay ledger files and the existing `replay-<run-id>-*.jsonl` glob still matches | `owner_role: control.coordinator` |
| R-FIN-027 | the `replay-determinism` job stanza in `.github/workflows/run-evidence-gates.yml` (separate job on `ubuntu-latest` with Python 3.11 + Node 20, `fetch-depth: 0`, `RERUNS=3`, `python -m unittest scripts.test_replay_determinism`, plus a failure-only `actions/upload-artifact` step for `artifacts/failbundles/`) | `DEC-FIN-008-chip-map-dedicated-determinism-fixture.md` | a CI run shows the `replay-determinism` job green on every PR and push to main with no escape hatch on any step | `owner_role: control.coordinator` |
| R-FIN-028 | the `cowos-l-bottleneck` scenario entry in `src/lib/scenarios.ts` (id, label, description, `impactOn` list, `edgeImpact` callback bumping `packages-for` / `supplies-substrates` / `manufactures-for` edges into the two accelerator platform rows, `nodeAttributeImpact` callback adding the six-month lead-time bump on TSMC plus ABF-substrate subtype nodes) plus the toggle row rendered by `src/components/ScenarioControls.tsx` | `DEC-FIN-009-additional-scenarios.md` | the scenario fires under `npm test` (`src/lib/scenarios.test.ts` asserts the multiplier target set, the edge-strength bumps, and the lead-time bumps) and the scenario toggle renders in the app shell | `owner_role: control.coordinator` |
| R-FIN-029 | the `lithography-equipment-constraint` scenario entry in `src/lib/scenarios.ts` (id, label, description, `impactOn` list, `edgeImpact` callback bumping `supplies-equipment` edges from the four lithography suppliers into the three leading-edge foundries, `nodeAttributeImpact` callback adding the nine-month bump on ASML plus the six-month bump on Lasertec) plus the toggle row rendered by `src/components/ScenarioControls.tsx` | `DEC-FIN-009-additional-scenarios.md` | the scenario fires under `npm test` (`src/lib/scenarios.test.ts` asserts the multiplier target set, the edge-strength bumps, and the lead-time bumps) and the scenario toggle renders in the app shell | `owner_role: control.coordinator` |
| R-FIN-030 | the lead-time bump folding plus the `scenarioEdgePressure` factor in `src/lib/scoring.ts::chokepointScore` (reading `scenarioLeadTimeBumpMonths` and `scenarioEdgeWeightMultiplier` from `src/lib/scenarios.ts`) plus the test fixture at `src/lib/scenarios.test.ts` | `DEC-FIN-009-additional-scenarios.md` | the scoring change folds the bumps without changing the baseline score (the test fixture asserts `scenarioEdgePressure` returns 1.0 on the empty scenario set) and the chokepoint score deltas read sensibly when each scenario toggles | `owner_role: control.coordinator` |
| R-FIN-031 | `scripts/test_chaos_run_evidence.py` (seven mutation classes M1..M7 mapped to the validator invariants: M1/M2 hash-agreement, M3 gate-results agreement, M4 required terminal event, M5 typed-payload `oneOf` on `pipeline.start`, M6 fields_populated agreement, M7 required-for-done `sandbox_image_ref`; each test loads the canonical sample, mutates an in-memory copy, writes to a `TemporaryDirectory`, monkey-patches the validator module's path constants, runs `main`, and asserts exit 1 plus the right check name in stderr) | `DEC-FIN-010-chip-supply-chain-map-chaos-test-suite.md` | `python -m unittest scripts.test_chaos_run_evidence` passes with all seven mutation classes asserting the validator catches each mutation against the canonical sample | `owner_role: control.coordinator` |
| R-FIN-032 | the `ChaosSanityCheck.test_unmutated_canonical_pair_passes` method plus the `MutationCoverageManifest.test_seven_mutation_classes_present` method in `scripts/test_chaos_run_evidence.py` (the manifest walks the module globals for `M<digit>` `TestCase` subclasses and pins the count at seven against a documented `EXPECTED_MUTATION_CLASSES` tuple) | `DEC-FIN-010-chip-supply-chain-map-chaos-test-suite.md` | the sanity test asserts the unmutated canonical pair validates clean inside the harness and the manifest test fails at test time when the class count drifts from seven | `owner_role: control.coordinator` |
| R-FIN-033 | the `chaos_run_evidence_tests` step in `.github/workflows/gates.yml` plus the `chaos-validation` job stanza in `.github/workflows/run-evidence-gates.yml` (separate job on `ubuntu-latest` with Python 3.11, `jsonschema` install, `python -m unittest scripts.test_chaos_run_evidence`) | `DEC-FIN-010-chip-supply-chain-map-chaos-test-suite.md` | a CI run shows the chaos step green in `Gates` and the `chaos-validation` job green in `Run-evidence gates` on every PR and push to main with no escape hatch on either step | `owner_role: control.coordinator` |
| R-FIN-034 | `ops/schemas-cache/decision.schema.json`, `ops/schemas-cache/dream-output.schema.json`, and `ops/schemas-cache/run.schema.json` (each carries the four optional fields `systems_map`, `transferable_principle`, `falsification_test`, `adoption_ladder` byte-for-byte mirrored from the athena-site sources) | `DEC-FIN-011-systems-thinking-discipline-adoption.md` | `python scripts/check_schema_cache_freshness.py` exits 0 and each cached schema's `properties` block carries the four new fields with the DEC-CDCP-020 shape | `owner_role: control.coordinator` |
| R-FIN-035 | the "Systems-thinking discipline (per DEC-CDCP-020)" section in `.agents/AGENTS.md` (sits before the "Coding style" section, names the four fields, the optional-with-warning contract, the 30-day ratchet, and the pure-design escape hatch) | `DEC-FIN-011-systems-thinking-discipline-adoption.md` | a reviewer reading AGENTS.md before acting on the repo encounters the four-field discipline as a top-level rule, not buried in a sub-bullet | `owner_role: control.coordinator` |
| R-FIN-036 | the `SYSTEMS_THINKING_FIELDS` tuple plus the warnings block in `scripts/validate_decisions.py::main` (walks every DEC with `status: approved`, accumulates missing-field warnings, prints them to stderr after the violations block, keeps exit code 0 unless the schema validation fails) | `DEC-FIN-011-systems-thinking-discipline-adoption.md` | `python scripts/validate_decisions.py` against the current decisions corpus prints the warning count on stderr, names each missing-field DEC, and exits 0 because no schema violations are present | `owner_role: control.coordinator` |
| R-FIN-037 | the retrofitted four-field blocks in the front-matter of `decisions/DEC-FIN-008-chip-map-dedicated-determinism-fixture.md`, `decisions/DEC-FIN-009-additional-scenarios.md`, and `decisions/DEC-FIN-010-chip-supply-chain-map-chaos-test-suite.md` (each carries `systems_map`, `transferable_principle`, `falsification_test`, and `adoption_ladder` with substantive content; the four sub-properties of `adoption_ladder` are populated on each) | `DEC-FIN-011-systems-thinking-discipline-adoption.md` | `python scripts/validate_decisions.py` reports 15 missing-field warnings against the post-retrofit corpus of 19 DECs; four DECs populate the fields (DEC-FIN-008, DEC-FIN-009, DEC-FIN-010, and the self-applying DEC-FIN-011) | `owner_role: control.coordinator` |
