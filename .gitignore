# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Python bytecode cache from the validator test suite.
__pycache__/
*.pyc

# Watchlist export output (the canonical sample lives in ops/run-records/
# + ops/event-ledger/; the export pipeline regenerates the packet file
# on every run, so the packet itself is not source-controlled).
ops/exports/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
